
services:
  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    volumes:
      - ./backend/database.db:/code/database.db
    ports:
      - "4000:4000"
      - "8001-8020:8001-8020" 
  


